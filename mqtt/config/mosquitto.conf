allow_anonymous true

listener 1883
protocol mqtt

# Optional WebSockets listener for tools that speak MQTT over WS
listener 9001
protocol websockets

persistence true
persistence_file mosquitto.db

# Log to stdout so `docker compose logs -f mosquitto` shows activity
log_dest stdout