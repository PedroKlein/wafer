[package]
name = "pass-through-transform"
version = "0.1.0"
edition = "2021"
description = "Pass-through transform plugin for WAFER pipeline"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"

[profile.release]
opt-level = "s"
lto = true
