[package]
name = "wafer-pass-through"
version = "0.1.0"
edition = "2024"
description = "Pass-through transform plugin for WAFER pipeline"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.53"

[profile.release]
opt-level = "s"
lto = true
strip = "debuginfo"
